/**
 * Init command for Claude-Flow
 * Consolidates initialization functionality
 */

// Re-export the init functionality from the appropriate location
export { initCommand } from "../init/index.ts"; 